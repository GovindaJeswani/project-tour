header.header
  nav.nav.nav--tours
    a.nav__el(href='/') HORIZON TOURs
  .header__logo
    img(src='/img/tour-big.png' alt='Horizon logo')
    //- img(src='/img/pin.png' alt='Horizon logo')
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el.nav__el--logout(href="/my-tours") My Bookings     
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up